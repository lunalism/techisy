// Supabase client configuration
// Note: For this project, we primarily use Prisma for database access.
// Supabase is used as the PostgreSQL provider.
// If you need Supabase client features (auth, realtime, storage),
// install @supabase/supabase-js and configure here.

export const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL
export const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
